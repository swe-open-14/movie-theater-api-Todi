const express = require("express");
const app = express();
const port = 3000;

//TODO: Create your GET Request Route Below: 


app.listen(port, () => {
    console.log(`Listening at http://localhost:${port}/movie`);
})